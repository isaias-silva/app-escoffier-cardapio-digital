export interface IDishe {
    menuId: String
    restaurantId: string
    name: string
    price: number
    image: string
    mode: 'MORNNING'
    description: string
    categoryId: string
    galery: string[]
}